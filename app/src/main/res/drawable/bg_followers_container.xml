<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo gris oscuro -->
    <solid android:color="#22262B"/>

    <!-- Radio grande para forma de píldora (siempre > alto/2) -->
    <corners android:radius="999dp"/>

    <!-- Borde verde-amarillo -->
    <stroke
        android:width="2dp"
        android:color="#D9E46C"/>

    <!-- Espacio interno -->
    <padding
        android:left="16dp"
        android:top="8dp"
        android:right="16dp"
        android:bottom="8dp"/>
</shape>
